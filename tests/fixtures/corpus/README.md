# Fixture corpus

Sidecars are FAKE (`{"over": …}`), verified only through the monkeypatched boundary in `tests/test_corpus.py`. Real sigstore verification rejects them — by design. Regenerate with `tests/fixtures/gen_corpus.py`.
